#pragma once

#include <SDL2/SDL.h>
#include <SDL2_image/SDL_image.h>

#include "GameObject.h"

class Obstacle : public GameObject
{
private:

protected:

public:
    Obstacle();
    virtual ~Obstacle();
};
